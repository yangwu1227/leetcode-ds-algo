// running_sum_1d.hpp
#ifndef RUNNING_SUM_1D_HPP
#define RUNNING_SUM_1D_HPP
#include <vector>

/**
 * @brief Given an array `nums`, the running sum of the array is defined as `runningSum[i] = sum(nums[0]…nums[i])`.
 * 
 * @param nums A vector of integers
 * @return void Modifies the vector in place
 */
void runningSum(std::vector<int> &nums);

#endif
