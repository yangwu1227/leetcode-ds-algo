// move_zeros.hpp
#ifndef MOVE_ZEROS_HPP
#define MOVE_ZEROS_HPP
#include <vector>

/**
 * @brief Move all zeros to the end of the array while maintaining the relative order of the non-zero elements.
 * 
 * @param nums The array of integers.
 * @return void
 */
void moveZeros(std::vector<int> &nums);

#endif
