// sorted_squares.hpp
#ifndef SORTED_SQUARES_HPP
#define SORTED_SQUARES_HPP
#include <vector>

/**
 * @brief Given an integer array nums sorted in non-decreasing order, return an array of the squares of each number sorted in non-decreasing order.
 *
 * @param nums Input vector of sorted (ascending) integers.
 * @return std::vector<int> Vector of squares of each number sorted in non-decreasing order.
 */
std::vector<int> sortedSquares(std::vector<int> nums);

#endif
