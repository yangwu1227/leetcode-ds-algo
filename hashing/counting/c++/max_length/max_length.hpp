// max_length.hpp
#ifndef MAX_LENGTH_HPP
#define MAX_LENGTH_HPP
#include <vector>

/**
 * @brief Given a binary array `nums`, return the maximum length of a contiguous subarray with an equal number of `0` and `1`.
 *
 * @param nums A binary vector containing 0's and 1's
 * @return int Maximum length of a continuous subarray
 */
int findMaxLength(const std::vector<int> &nums);

#endif
