// max_subarray_unique.hpp
#ifndef MAX_SUBARRAY_UNIQUE_HPP
#define MAX_SUBARRAY_UNIQUE_HPP
#include <vector>

/**
 * @brief Given an array of positive integers `nums`, find the maximum sum among all unique subarrays.
 *
 * @param nums A vector of positive integers
 * @return int The maximum sum of the unique subarray
 */
int maxSubarrayUnique(const std::vector<int> &nums);

#endif
