// subarray_sum.hpp
#ifndef SUBARRAY_SUM_HPP
#define SUBARRAY_SUM_HPP
#include <vector>

/**
 * @brief Given an integer array `nums` and an integer `k`, find the number of subarrays whose sum is equal to `k`.
 *
 * @param nums A vector of integers
 * @param k An integer contraint
 * @return int Number of subarrays whose sum equal `k`
 */
int subarraySum(const std::vector<int> &nums, int k);

#endif
