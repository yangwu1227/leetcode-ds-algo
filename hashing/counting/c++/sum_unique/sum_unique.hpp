// sum_unique.hpp
#ifndef SUM_UNIQUE_HPP
#define SUM_UNIQUE_HPP
#include <vector>

/**
 * @brief Given an integer array `nums`, return the sum of all the unique elements of `nums`.
 *
 * @param nums A vector a integers
 * @return int Sum of all unique elements
 */
int sumUnique(const std::vector<int> &nums);

#endif
