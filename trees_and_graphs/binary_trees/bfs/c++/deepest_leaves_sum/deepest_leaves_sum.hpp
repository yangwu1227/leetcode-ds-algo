// deepest_leaves_sum.hpp
#ifndef DEEPEST_LEAVES_SUM_HPP
#define DEEPEST_LEAVES_SUM_HPP
#include "tree_node.hpp"

/**
 * @brief Given a binary tree, return the sum of values of its deepest leaves.
 *
 * @param root The root of the binary tree
 *
 * @return int The sum of values of the deepest leaves
 */
int deepestLeavesSum(TreeNode::ptr &root);

#endif
