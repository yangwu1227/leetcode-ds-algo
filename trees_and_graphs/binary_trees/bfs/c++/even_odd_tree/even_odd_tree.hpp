// even_odd_tree.hpp
#ifndef EVEN_ODD_TREE_HPP
#define EVEN_ODD_TREE_HPP
#include "tree_node.hpp"

/**
 * @brief Check if a binary tree is an Even-Odd tree.
 *
 * @param root A unique pointer to the root node of the binary tree
 *
 * @return `true` if the tree is an Even-Odd tree, `false` otherwise
 */
bool checkEvenOdd(TreeNode::ptr &root);

#endif
