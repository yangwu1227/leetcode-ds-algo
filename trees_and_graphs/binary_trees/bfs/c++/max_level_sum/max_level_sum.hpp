// max_level_sum.hpp
#ifndef MAX_LEVEL_SUM_HPP
#define MAX_LEVEL_SUM_HPP
#include "tree_node.hpp"

/**
 * @brief Find the smallest level in a binary tree with the maximum sum of node values.
 *
 * @param root A unique pointer to the root node of the binary tree
 *
 * @return int  The smallest level (1-indexed) with the maximum sum of node values
 */
int maxLevelSum(TreeNode::ptr &root);

#endif
