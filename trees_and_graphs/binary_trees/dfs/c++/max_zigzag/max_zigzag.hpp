// max_zigzag.hpp
#ifndef MAX_ZIGZAG_HPP
#define MAX_ZIGZAG_HPP
#include "tree_node.hpp"

/**
 * @brief Find the maximum length of a ZigZag path in the binary tree.
 *
 * @param root A unique pointer to the root of the binary tree
 * @return int The length of the longest ZigZag path in the tree
 */
int maxZigZag(TreeNode::ptr &root);

#endif
