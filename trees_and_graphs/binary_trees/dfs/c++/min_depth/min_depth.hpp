// min_depth.hpp
#ifndef MIN_DEPTH_HPP
#define MIN_DEPTH_HPP
#include "tree_node.hpp"

/**
 * @brief Calculate the minimum depth of a binary tree.
 *
 * @param root A unique pointer to the root node of the binary tree.
 *
 * @return int The minimum depth of the binary tree. If the tree is empty, returns 0.
 */
int minDepth(TreeNode::ptr &root);

#endif
